/**
 * src/server/getCurrentUser.ts
 *
 * Re-exported at this path to match the requested folder architecture
 * (`server/getCurrentUser.ts`, `server/requireUser.ts`). The actual
 * implementation lives in `src/auth/server.ts` alongside the rest of the
 * Auth.js wiring — kept together so there's exactly one file that touches
 * `next-auth` directly.
 */
export { getCurrentUser } from "@/src/auth/server";
